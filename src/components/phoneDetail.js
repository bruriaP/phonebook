import React from "react";

export function PhoneRec(props) {
  return (
    <ul>
        <li>one Record</li>
          {/* {props.details.map(id => (
        <li key={id}>{`${id}`}</li>
      ))} */}
    </ul>
  );
}

export default PhoneRec;